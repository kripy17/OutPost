server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing — every path falls back to index.html (React Router owns it).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
