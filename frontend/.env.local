# OutPost preview/dev config — the frontend talks to the backend directly (no Vite proxy).
# Local preview uses port 8001 because 8000 is taken by another project on this machine.
VITE_API_URL=http://localhost:8001
